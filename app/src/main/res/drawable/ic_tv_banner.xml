<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background layer -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/ic_launcher_background_alien" />
        </shape>
    </item>
    <!-- Centered Alien Icon -->
    <item
        android:gravity="center"
        android:drawable="@mipmap/ic_launcher_alien"
        android:width="180dp"
        android:height="180dp" />
</layer-list>